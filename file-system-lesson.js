const fs = require('fs');
const path = require('path');
/*
fs.mkdir(path.join(__dirname, 'notes'), (error) =>{
    if(error){
        throw  new Error(error)
    }
    console.log('folder created');
});
fs.writeFile(path.join(__dirname, 'notes', 'first-note.txt'),
    'some simple note',
    (err) => {
        if(err) throw new Error(err);
        console.log('file created');
    })*/
/*fs.readFile(
    path.join(__dirname, 'notes', 'first-note.txt'),
    (err, data) => {
        if(err) throw err;
        console.log(data.toString());
    }
)*/
/*fs.rename(
    path.join(__dirname, 'notes', 'first-note.txt'),
    path.join(__dirname, 'notes', 'changed-file.txt'),
    (err) => {
        if(err) throw err;
        console.log('the file renamed');
    }
)*/
